version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - sudo rm -rf pcb_front scripts appspec.yml .git
  build:
    commands:
      - echo Build started on `date`      
  post_build:
    commands:
      - echo Build completed on `date`
      
artifacts:
  files:
    - '**/*'
